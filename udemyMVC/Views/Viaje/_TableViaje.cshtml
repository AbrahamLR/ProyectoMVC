@using udemyMVC.Models
@model List<ViajeCLS>
<table class="table-bordered table-hover">
    <tr>
        <td>@Html.LabelFor(p => Model[0].iidViaje) </td>
        <td>@Html.LabelFor(p => Model[0].nombreLugarOrigen)</td>
        <td>@Html.LabelFor(p => Model[0].nombreLugarDestino)</td>
        <td>@Html.LabelFor(p => Model[0].nombreBus)</td>
        <td>Operaciones</td>
    </tr>
    @foreach (var item in Model)
    {
    <tr>
        <td>@item.iidViaje</td>
        <td>@item.nombreLugarOrigen</td>
        <td>@item.nombreLugarDestino</td>
        <td>@item.nombreBus</td>
        @*<td>
            @Html.ActionLink(" ", "Editar", "Viaje", new { id = @item.iidViaje }, new { @class = "glyphicon glyphicon-edit bnt-primary" }) 
            @Html.ActionLink(" ", "Eliminar", "Viaje", new { id = @item.iidViaje }, new { @class = "glyphicon glyphicon-trash btn-danger" })
        </td>*@
        <td>
            <button type="button" onclick="Editar(@item.iidViaje)" class="btn btn-primary glyphicon glyphicon-edit" data-toggle="modal" data-target="#exampleModal">
                Editar
            </button>
        </td>
        <td>
            <button type="button" onclick="Eliminar(@item.iidViaje)" class="btn btn-danger glyphicon glyphicon-trash" data-toggle="modal" data-target="#ModalElimina">
                Eliminar
            </button>
        </td>

    </tr>

    }
</table>
